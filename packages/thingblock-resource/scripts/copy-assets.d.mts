// Type declaration for copy-assets.mjs, consumed only by test/copy-assets.test.ts. TypeScript
// resolves this sibling `.d.mts` for the `.mjs` import automatically; the script itself stays plain
// ESM, matching the other files in scripts/.
export function copyRawAssets(root: string, dir: string, out: string): void
